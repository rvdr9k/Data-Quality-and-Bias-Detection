# Setup

## Requirements

- Python 3.9+
- pip

## Install dependencies

```bash
pip install -r requirements.txt

Run backend
From backend/: uvicorn main:app --reload

Run frontend
From frontend/: Streamlit run app.py

### The backend must be running before starting the frontend.
